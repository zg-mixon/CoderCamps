'use strict';

let wordList = ['cow', 'pig', 'jerk', 'norman', 'blob'];

let random: number = Math.floor(Math.random() * wordList.length);

let word: string = wordList[random];

let newword: string = 